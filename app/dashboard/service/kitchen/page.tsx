// ─── /dashboard/service/kitchen ──────────────────────────────────────────────
// Canonical Service → Kitchen landing.
// Redirects to the Create Order page (same behaviour as /dashboard/kds).
export { default } from '@/app/dashboard/kds/page'
